# SoundController USB Firmware 0.1.3

- Adds an explicit Windows app connected handshake.
- Still accepts the existing full six-slot sync for compatibility.
- Keeps the OLED on `NO APP` / `WAITING` when stale HID output reports are replayed without the companion app running.
